exec dpd_exec {
    wait = yes
    input_pairs = request
    shell_escape = yes
    timeout = 3
    program = "%{config:confdir}/mods-config/realm/freeradius-naptr-to-home-server.sh -d %{config:confdir} %{request:Realm-Name}"
}
