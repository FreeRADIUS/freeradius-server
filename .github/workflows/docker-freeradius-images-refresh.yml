name: Docker FreeRADIUS images refresh

#
#  This workflow rebuilds and publishes the
#  `freeradius4-{crossbuild,profiling-deps,service}` images.  Only the
#  multi-server tests, `docker-crossbuild.yml` and `docker-service.yml` read
#  the 'freeradius4' images.  The pipeline's build legs never read the
#  'freeradius4' images.  Keeping this stage in a separate workflow lets the
#  pipeline gate each leg on the refresh stage that the leg actually reads.
#  A build leg therefore does not wait for the slowest images in the registry.
#
#  `ci-pipeline.yml` calls this workflow when a commit changes the images.
#  `docker-ci-images-refresh.yml` calls this workflow on the nightly schedule
#  and on `workflow_dispatch`.  The registry must already hold the
#  `self-hosted-docker-{dind,cli}` job containers under the ref's tag, which
#  both callers guarantee by running the earlier stages of
#  `docker-ci-images-refresh.yml` first.
#
on:
  workflow_call:

concurrency:
  #
  #  Only one refresh of the 'freeradius4' images runs at a time.
  #  `cancel-in-progress: false` lets an in-flight run finish rather than
  #  having a push cancel the run.  An interrupted refresh leaves the
  #  registry partially updated, whereas a queued second run re-publishes
  #  everything cleanly after the first run finishes.
  #
  group: docker-freeradius-images-refresh
  cancel-in-progress: false

jobs:
  #
  #  The `common_vars` job determines which registry holds the continuous
  #  integration (CI) images.  See `.github/workflows/common_vars.yml`.
  #
  common_vars:
    name: Common vars
    uses: ./.github/workflows/common_vars.yml

  #
  #  This job periodically refreshes the FreeRADIUS base image chain in
  #  the internal registry, and builds the chain for each supported
  #  distro in parallel:
  #
  #    crossbuild        `FROM` the upstream OS base, full toolchain +
  #                      FreeRADIUS build-deps
  #    profiling-deps    `FROM` `crossbuild:latest`, toolchain layer
  #                      (valgrind / kcachegrind / dbgsym / FlameGraph /
  #                      inferno / pprof)
  #    service           `FROM` the same upstream OS base as 'crossbuild'
  #                      (NOT from 'crossbuild') - the FreeRADIUS
  #                      debian/rpm package build apt-installs build
  #                      deps for itself, and layering on 'crossbuild'
  #                      would bloat the runtime image with toolchain
  #                      artefacts.
  #
  #  The job pushes each of the three images under the repository's path,
  #  tagged for the ref being built: `latest` on the default branch, the
  #  branch's own tag elsewhere.  `common_vars.yml` decides the tag.
  #
  #  This job deliberately does NOT publish the 'profiling' layer
  #  (FreeRADIUS compiled with callgrind-friendly `CFLAGS` on top of
  #  'profiling-deps').  The 'profiling' layer is per-commit by design.  We
  #  want to profile specific commits, not whatever `master` HEAD was at
  #  refresh time.  Multi-server profiling tests pull 'profiling-deps' and
  #  build the 'profiling' layer locally per commit.
  #
  process-freeradius-images:
    needs: common_vars
    timeout-minutes: 90

    env:
      DOCKER_REGISTRY: ${{ needs.common_vars.outputs.docker_registry }}
      IMAGE_PREFIX: ${{ needs.common_vars.outputs.image_prefix }}
      IMAGE_TAG: ${{ needs.common_vars.outputs.image_tag }}

    runs-on: self-hosted
    if: ${{ (github.repository_owner == 'FreeRADIUS' || github.event_name == 'workflow_dispatch') && needs.common_vars.outputs.selfhosted == '1' }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu24, ubuntu26, debian13, debiansid, rocky9, rocky10]

    services:
      dind:
        image: ${{ needs.common_vars.outputs.image_prefix }}self-hosted-docker-dind:${{ needs.common_vars.outputs.image_tag }}
        options: --privileged
        env:
          DOCKER_TLS_CERTDIR: ""
          NO_PROXY: "*.networkradius.com,127.0.0.1"
        volumes:
          - ${{ github.workspace }}:/workspace

    container:
      image: ${{ needs.common_vars.outputs.image_prefix }}self-hosted-docker-cli:${{ needs.common_vars.outputs.image_tag }}
      env:
        DOCKER_HOST: tcp://dind:2375
        NO_PROXY: dind,*.networkradius.com,127.0.0.1
      volumes:
        - /usr/local/share/ca-certificates/networkradius.com.crt:/usr/local/share/ca-certificates/networkradius.com.crt:ro
        - ${{ github.workspace }}:/workspace

    defaults:
      run:
        shell: bash
        working-directory: /workspace

    name: "freeradius4-{crossbuild,profiling-deps,service}-${{ matrix.os }}"

    steps:

      - uses: actions/checkout@v6
        with:
          lfs: false

      - name: setup-dind
        uses: ./.github/actions/setup-dind

      - name: Login to Docker Hub (via dind)
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_READ_USER }}
          password: ${{ secrets.DOCKERHUB_READ_KEY }}

      #
      #  The read-only login covers any `DOCKER_REGISTRY` pull-through
      #  misses during the build (the 'crossbuild' Dockerfile pulls the upstream
      #  OS base with `FROM`, not the internal registry).
      #
      - name: Login to internal Docker registry (read, via dind)
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_REPO_USERNAME }}
          password: ${{ secrets.DOCKER_REPO_PASSWORD }}
          registry: ${{ env.DOCKER_REGISTRY }}

      #
      #  Build 'crossbuild' + 'profiling-deps' + 'service'. 'crossbuild'
      #  and 'service' both pull the upstream OS base with `FROM`. The
      #  'profiling-deps' Dockerfile pulls `crossbuild:latest`, which `DOCKER_BUILD` writes locally
      #  as part of the 'crossbuild' build, so the chain resolves in
      #  order without a registry pull between layers.
      #
      #  `docker.mk` encodes the chain as stamp prerequisites, so one
      #  make run with `-j` builds the independent 'service' image
      #  alongside the 'crossbuild' > 'profiling-deps' chain.
      #
      #  `NOPULL=1` forces `docker.mk` to build from the local
      #  `Dockerfile` instead of pulling whatever the registry already
      #  holds for this ref.  Without `NOPULL=1` the refresh silently
      #  retags whatever the registry already published, which defeats
      #  the point of the workflow.
      #
      - name: Build crossbuild + profiling-deps + service images
        env:
          OS: ${{ matrix.os }}
          NOPULL: 1
        run: |
          make dockerfile.check
          make -j$(nproc) docker.profiling-deps.$OS docker.service.$OS

      - name: Show docker build logs
        if: failure()
        env:
          OS: ${{ matrix.os }}
        run: |
          for type in crossbuild profiling-deps service; do
            log="build/docker/build.$OS.$type"
            if [ -f "$log" ]; then
              echo "===== $log ====="
              cat "$log"
            fi
          done

      #
      #  Switch to push-capable credentials and publish the 'crossbuild',
      #  'profiling-deps' and 'service' images under this ref's tag. The
      #  per-commit 'profiling' layer stays local.
      #
      - name: Login to internal Docker registry for push (via dind)
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_REPO_UPDATE_USERNAME }}
          password: ${{ secrets.DOCKER_REPO_UPDATE_PASSWORD }}
          registry: ${{ env.DOCKER_REGISTRY }}

      - name: Push freeradius4-{crossbuild,profiling-deps,service}
        env:
          OS: ${{ matrix.os }}
        run: |
          for type in crossbuild profiling-deps service; do
            #  `docker.mk` always builds the local tag as `:latest`.
            local_tag="freeradius4-${type}/${OS}:latest"
            remote_tag="${IMAGE_PREFIX}freeradius4-${type}/${OS}:${IMAGE_TAG}"
            docker tag "${local_tag}" "${remote_tag}"
            for attempt in 1 2 3; do
              docker push "${remote_tag}" && break
              echo "Retrying push of ${remote_tag} after backoff, attempt $attempt failed"
              sleep 10
            done
          done
