name: CI DEB

on:
  workflow_call:

env:
  DEBIAN_FRONTEND: noninteractive

jobs:
  #
  #  The `common_vars` job emits different values depending on whether the run
  #  is occurring in the FreeRADIUS organisation or in a fork.  See
  #  `.github/workflows/common_vars.yml`.
  #
  common_vars:
    name: Common vars
    uses: ./.github/workflows/common_vars.yml


  #
  #  We do not want to consume many workers on each push, so we build the
  #  full suite of distros only during the scheduled or `ci-debug` run.  On
  #  each push we build just the 'bleeding-edge' distro.
  #
  #  The `set-matrix` job builds the matrix based on the event that triggered
  #  the run.  The `deb-build` job consumes the matrix.
  #
  set-matrix:
    needs: common_vars
    name: Setup build matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
    - id: set-matrix
      name: Setup the matrix
      run: |
        if [ "$GITHUB_EVENT_NAME" = "schedule" -o "$GITHUB_REF" = "refs/heads/ci-debug" ]; then
          M=$(cat <<EOF
          {
            "env": [
              { "NAME": "ubuntu-24.04", "OS": "ubuntu:24.04",    "BUILD_IMAGE": "${{ needs.common_vars.outputs.image_prefix }}self-hosted-ci-ubuntu24:${{ needs.common_vars.outputs.image_tag }}"   },
              { "NAME": "ubuntu-26.04", "OS": "ubuntu:26.04",    "BUILD_IMAGE": "${{ needs.common_vars.outputs.image_prefix }}self-hosted-ci-ubuntu26:${{ needs.common_vars.outputs.image_tag }}"   },
              { "NAME": "debian-13",    "OS": "debian:trixie",   "BUILD_IMAGE": "${{ needs.common_vars.outputs.image_prefix }}self-hosted-ci-debian13:${{ needs.common_vars.outputs.image_tag }}"   },
              { "NAME": "debian-sid",   "OS": "debian:sid",      "BUILD_IMAGE": "${{ needs.common_vars.outputs.image_prefix }}self-hosted-ci-debiansid:${{ needs.common_vars.outputs.image_tag }}"  }
            ]
          }
        EOF
          )
        else
          M=$(cat <<EOF
          {
            "env": [
              { "NAME": "debian-13",    "OS": "debian:trixie",   "BUILD_IMAGE": "${{ needs.common_vars.outputs.image_prefix }}self-hosted-ci-debian13:${{ needs.common_vars.outputs.image_tag }}" }
            ]
          }
        EOF
          )
        fi
        echo matrix=$M >> $GITHUB_OUTPUT


  deb-build:

    needs:
      - set-matrix
      - common_vars

    strategy:
      matrix: ${{ fromJson(needs.set-matrix.outputs.matrix) }}
      fail-fast: false

    runs-on: ${{ needs.common_vars.outputs.selfhosted == '1' && 'self-hosted' || 'ubuntu-latest' }}

    container:
      image: ${{ matrix.env.BUILD_IMAGE }}


    name: "DEB build"

    steps:

    - uses: actions/checkout@v6
      with:
        path: freeradius

    #
    #  The CI image includes the build dependencies, and
    #  `docker-ci-images-refresh.yml` refreshes the image when a packaging
    #  change lands on `master`.  A pull request's tests run on a Docker image
    #  that does not include dependencies added in the pull request, and the
    #  refreshed image arrives only after the pull request merges.  Top up
    #  build dependencies from the checked-out `debian/control` for such a run
    #  only.  The top-up is a no-op when the image is current.
    #
    - name: Top up build dependencies from debian/control
      if: ${{ needs.common_vars.outputs.images_changed == 'true' }}
      run: |
        touch -t 202001010000 debian/control
        debian/rules debian/control
        mk-build-deps -irt"apt-get -y --no-install-recommends" debian/control
      working-directory: freeradius

    - name: Build DEBs
      run: |
        make deb
      working-directory: freeradius

    - name: Collect DEBs
      run: |
        rm -rf debs
        mkdir debs
        mv *.deb debs

    #  The CI image already has `eapol_test`.  The stock image that the install
    #  test runs in has no `eapol_test`, so copy `eapol_test` into `debs/` beside
    #  the packages.  The install test moves `eapol_test` to `/usr/local/bin`,
    #  and `scripts/ci/package-test.mk` looks `eapol_test` up on `PATH`.
    - name: Stage eapol_test for the install test
      run: cp /usr/local/bin/eapol_test debs/eapol_test

    - name: Store DEBs
      uses: actions/upload-artifact@v6
      with:
        name: debs-${{ matrix.env.NAME }}
        path: debs
        retention-days: 1

    #
    #  If the CI has failed and the branch is `ci-debug` then the workflow starts
    #  a tmate session.  See the tmate comment in `ci.yml` for the walkthrough.
    #
    - name: "Debug: Start tmate"
      uses: mxschmitt/action-tmate@v3
      with:
        limit-access-to-actor: true
        sudo: false
      if: ${{ github.ref == 'refs/heads/ci-debug' && failure() }}


  #
  #  The `deb-test` job runs the 'post-install' tests against the FreeRADIUS
  #  packages that we have just built.  The tests run in a clean environment
  #  consisting of only the base OS and the package dependencies.
  #
  deb-test:

    needs:
      - set-matrix
      - deb-build
      - common_vars

    strategy:
      matrix: ${{ fromJson(needs.set-matrix.outputs.matrix) }}
      fail-fast: false

    runs-on: ${{ needs.common_vars.outputs.selfhosted == '1' && 'self-hosted' || 'ubuntu-latest' }}

    container:
      #
      #  `deb-test` runs on a stock distro image so the install test exercises a
      #  clean operating system.  `docker_prefix` points the name at the copy
      #  that the organisation keeps of the same image.
      #  `docker-ci-images-refresh.yml` puts the copy in the organisation's
      #  registry.  The copy is the identical image under another name, so
      #  the install test still exercises a clean operating system.  The
      #  organisation's build machines share a few outbound addresses.
      #  Pulling the copy also keeps the build machines away from Docker
      #  Hub's allowance of 100 downloads every six hours per address.
      #
      #  A fork has no such copy, so `docker_prefix` is empty and the name
      #  reaches Docker Hub unchanged.  GitHub-hosted machines spread over a
      #  large pool of addresses, so the allowance is not a problem on
      #  GitHub-hosted machines.
      #
      image: ${{ needs.common_vars.outputs.docker_prefix }}${{ matrix.env.OS }}

    name: "DEB install test"

    steps:

    - name: Load DEBs
      uses: actions/download-artifact@v8
      with:
        name: debs-${{ matrix.env.NAME }}

    - name: Package manager performance and stability improvements
      run: |
        if [ -f "/etc/apt/sources.list" ]; then
        sed -i 's/deb.debian.org/debian-archive.trafficmanager.net/' /etc/apt/sources.list
        sed -i 's/archive.ubuntu.com/azure.archive.ubuntu.com/' /etc/apt/sources.list
        fi
        echo 'Acquire::Retries "10";' > /etc/apt/apt.conf.d/80-retries
        echo 'force-unsafe-io' > /etc/dpkg/dpkg.cfg.d/02speedup
        echo 'man-db man-db/auto-update boolean false' | debconf-set-selections
        apt-get update

    # `procps` for `pkill`, `curl`+`gnupg2` for `apt-key`, `ca-certificates` for curl
    - name: Install system utilities
      run: |
        apt-get install -y --no-install-recommends procps curl gnupg2 ca-certificates lsb-release

    - name: NetworkRADIUS signing key
      run: |
        install -d -o root -g root -m 0755 /etc/apt/keyrings
        curl -s 'https://packages.inkbridgenetworks.com/pgp/packages.networkradius.com.asc' | tee /etc/apt/keyrings/packages.networkradius.com.asc > /dev/null

    - name: Set up NetworkRADIUS extras repository
      run: |
        DIST=$(lsb_release -is | tr '[:upper:]' '[:lower:]')
        RELEASE=$(lsb_release -cs)
        if [ "$RELEASE" = "n/a" ]; then
            RELEASE=$(cat /etc/debian_version | awk -F \/ '{ print $(NF) }')
        fi
        [ "$RELEASE" != "forky" ] || RELEASE=sid
        echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/packages.networkradius.com.asc] http://packages.networkradius.com/extras/${DIST}/${RELEASE} ${RELEASE} main" \
          > /etc/apt/sources.list.d/networkradius-extras.list

    - name: Update apt repository lists
      run: apt-get update

    - name: Install DEBs
      run: |
        find . -maxdepth 1 -name '*.deb' | xargs apt-get install -y --no-install-recommends

    - name: Config test
      run: |
        freeradius -XC

    #
    #  We now run some post-install tests that need the source tree
    #
    - name: Install pre-built eapol_test
      run: |
        apt-get install -y libssl1.? libdbus-1-? libnl-3-200 libnl-genl-3-200
        mv eapol_test /usr/local/bin
        chmod +x /usr/local/bin/eapol_test

    - uses: actions/checkout@v6
      with:
        path: freeradius

    - name: Run the post-install test target
      run: |
        make -C raddb/certs Q=@
        touch Make.inc
        make -f scripts/ci/package-test.mk package-test
      working-directory: freeradius

    #  The `deb-test` job checks out the source into `freeradius/`, so the
    #  `collect-core-dumps` action path starts with `freeradius/` and the action
    #  runs from `freeradius/`.
    - name: Collect core dumps
      if: ${{ always() }}
      uses: ./freeradius/.github/actions/collect-core-dumps
      with:
        name: ${{ matrix.env.NAME }}
        working-directory: freeradius

    - name: Upload radius logs on failure
      if: ${{ failure() }}
      uses: actions/upload-artifact@v6
      with:
        name: radius-logs-${{ matrix.env.NAME }}
        path: |
          /var/log/freeradius
          freeradius/build/tests/eapol_test
        retention-days: 30

    #
    #  See the tmate comments above
    #
    - name: "Debug: Package dependencies for tmate"
      run: |
        apt-get install -y --no-install-recommends xz-utils
      if: ${{ github.ref == 'refs/heads/ci-debug' && failure() }}

    - name: "Debug: Start tmate"
      uses: mxschmitt/action-tmate@v3
      with:
        limit-access-to-actor: true
        sudo: false
      if: ${{ github.ref == 'refs/heads/ci-debug' && failure() }}
