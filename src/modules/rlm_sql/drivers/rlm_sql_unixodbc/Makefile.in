include ../../../../../Make.inc

TARGET		= @targetname@
SRCS		= sql_unixodbc.c
HEADERS		= sql_unixodbc.h
RLM_SQL_CFLAGS	= @sql_unixodbc_cflags@ $(INCLTDL)
RLM_SQL_LIBS	= @sql_unixodbc_ldflags@

include ../rules.mak

$(DYNAMIC_OBJS): $(HEADERS) 
