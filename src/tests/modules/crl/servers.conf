server crl {
	namespace = crl

	crl {
		ca_file = raddb/certs/rsa/ca.pem
		force_refresh = 5s
	}

	recv CRL-Fetch {
		reply.CRL-Data = %file.cat("$ENV{OUTPUT_DIR}/ca.crl", 16384)
		ok
	}

	send Fetch-OK {
		ok
	}

	send Fetch-Fail {
		ok
	}
}
