crl {
	virtual_server = crl
}

linelog cacnf {
	destination = file
	file {
		filename = "$ENV{OUTPUT_DIR}/ca%{clientno}.cnf"
	}
}

linelog clientcnf {
	destination = file
	file {
		filename = "$ENV{OUTPUT_DIR}/client%{clientno}.cnf"
	}
}

delay {
	delay = 1.0s
}
