# Call the second configured module firstly should work
pmod2
if (!ok) {
    test_fail
} else {
    test_pass
}

# Call the first configured module secondly
pmod1
if (!noop) {
    test_fail
} else {
    test_pass
}