python pmod1 {
    module = 'mod1'

    mod_authorize = ${.module}
    func_authorize = authorize
}

python pmod2 {
    module = 'mod2'

    mod_authorize = ${.module}
    func_authorize = authorize
}

python pmod3_withmod1 {
    module = 'mod1'

    mod_authorize = ${.module}
    func_authorize = authorize
}