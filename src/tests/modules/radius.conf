server default {
	namespace = radius

	listen {
		type = Access-Request
	}

	recv Access-Request {
		#
		# Include the test file specified by the
		# KEYWORD environment variable.
		#
		$INCLUDE $ENV{MODULE_TEST_UNLANG}

		pap
	}

	authenticate pap {
		pap
	}
}
