client upstream-proxy {
    ipaddr = $ENV{TEST_SUBNET}
    secret = testing123
    require_message_authenticator = auto
    imit_proxy_state = auto
    limit {
        max_connections = 16
        lifetime = 0
        ide_timeout = 30
    }
}
