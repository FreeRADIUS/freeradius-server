#
#  PRE: update if
#

#
#  Malformed NAS-IP-Address
#
&request.raw.4 = 0x010203

debug_request

if (!&raw.4) {
	&reply.Filter-Id := "Cannot find newly added raw attribute"
}
else {
	if (&raw.4 != 0x010203) {
		&reply.Filter-Id := "raw.4 is not 0x0120203"
	}
}

success
