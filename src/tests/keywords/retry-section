# PRE: update if

&request.Tmp-Integer-0 := 0

group {
	&request.Tmp-Integer-0 := "%{expr: &request.Tmp-Integer-0 + 1}"

	noop
	actions {
		noop = retry

		retry {
			max_rtx_count = 3
		}
	}
}

if (&request.Tmp-Integer-0 != 3) {
	test_fail
}

success
