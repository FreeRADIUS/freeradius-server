# PRE: switch
#
switch User-Name {
	case "harry" {
		update reply {
			Filter-Id := "harry"
		}
	}

	case "doug" {
		update reply {
			Filter-Id := "doug"
		}
	}

	case {
		update reply {
			Filter-Id := "filter"
		}
	}

}