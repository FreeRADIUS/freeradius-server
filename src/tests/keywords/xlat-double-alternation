#
# PRE: update
#

# Regression test for double consecutive alternations
&request.Tmp-uint64-0 := "%{%{request.Acct-Input-Octets}:-2}%{%{request.Acct-Input-Gigawords}:-1}"

if (&request.Tmp-uint64-0 != 21) {
	fail
}

success
