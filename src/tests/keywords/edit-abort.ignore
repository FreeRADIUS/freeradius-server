#
#  PRE: edit
#
&request.Tmp-Integer-0 := 4
&request.Tmp-Integer-1 := 5

#
#  First modification succeeds.  The second modification fails, which
#  also reverts the changes to the first one.
#
&request.Tmp-Integer-0 += 5
&request.Tmp-Integer-1 -= 16	# Run-time underflow!

# try to see what's up
%(debug_attr:request[*])

#
#  The various edits above should have been reverted.
#
if (&request.Tmp-Integer-0 != 4) {
	test_fail
}

if (&request.Tmp-Integer-1 != 5) {
	test_fail
}
success
