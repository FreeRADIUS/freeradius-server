# PRE: if if-else
#
#  Matching "if" conditions
#
if (User-Name != "bob") {
	update reply {
		Filter-Id := "not bob"
	}
}
elsif (User-Name == "bob") {
	update reply {
		Filter-Id := "filter"
	}
}
else {
	update reply {
		Filter-Id := "last else"
	}
}
