#
# PRE: try load-balance
#
#  A load-balance block whose only child is a missing optional module.
#
#  The section is not empty in the configuration, so it is not ignored
#  when compiled.  After compilation it has no children, so running it
#  fails the request.
#
string marker

try {
	load-balance {
		-no_such_module
	}

	#
	#  Should not reach here
	#
	test_fail
}
catch fail {
	marker := "caught"
}

if (marker != "caught") {
	test_fail
}

success
