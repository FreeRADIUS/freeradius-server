#
#  Minimal radiusd.conf for testing update
#

raddb		= raddb
update		= src/tests/update

modconfdir	= ${raddb}/mods-config

#  Only for testing!
#  Setting this on a production system is a BAD IDEA.
security {
	allow_vulnerable_openssl = yes
}

$INCLUDE ./$ENV{PROTOCOL}.conf
